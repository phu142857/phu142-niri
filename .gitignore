/target
/result

.idea

# Cursor / AI agent tooling (local, per-machine)
.cursor/
.agents/
.claude/

# CodeGraph (local index/cache, per-machine)
.codegraph/
codegraph.db
codegraph.db-*

# Agent instruction files (local)
AGENTS.md
AGENT.md
AGENT/

# MCP (Model Context Protocol) config
.mcp/
mcp.json
**/mcp.json